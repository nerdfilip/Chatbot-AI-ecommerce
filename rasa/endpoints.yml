action_endpoint:
  url: "${ACTIONS_URL}/webhook"

tracker_store:
  type: SQL
  dialect: postgresql
  url: "${DB_HOST}"
  port: "${DB_PORT}"
  db: "${DB_NAME}"
  username: "${DB_USER}"
  password: "${DB_PASSWORD}"